diff --git a/gfx/video_driver.c b/gfx/video_driver.c
--- a/gfx/video_driver.c
+++ b/gfx/video_driver.c
@@ -38,6 +38,9 @@
 #ifdef HAVE_GFX_WIDGETS
 #include "gfx_widgets.h"
 #endif
+#ifdef HAVE_SPRUCE_IGM_SW
+#include "../spruce_igm_sw.h"
+#endif

 #ifdef HAVE_THREADS
 #include "video_thread_wrapper.h"
@@ -4088,7 +4091,11 @@
             pitch, runloop_idle);

 #ifdef HAVE_VIDEO_FILTER
-   if (render_frame && data && video_st->state_filter)
+   if (render_frame && data && video_st->state_filter
+#ifdef HAVE_SPRUCE_IGM_SW
+         && !spruce_igm_sw_is_active()
+#endif
+      )
    {
       unsigned output_width                             = 0;
       unsigned output_height                            = 0;
diff --git a/runloop.c b/runloop.c
index 679517b..17172ff 100644
--- a/runloop.c
+++ b/runloop.c
@@ -208,6 +208,9 @@
 #include "configuration.h"
 #include "list_special.h"
 #include "core_option_manager.h"
+#ifdef HAVE_SPRUCE_IGM_SW
+#include "spruce_igm_sw.h"
+#endif
 #ifdef HAVE_CHEATS
 #include "cheat_manager.h"
 #endif
@@ -6006,15 +6009,25 @@ static enum runloop_state_enum runloop_check_state(
 
       if (pressed && !old_pressed)
       {
-         bool core_is_running    = runloop_st->flags & RUNLOOP_FLAG_CORE_RUNNING;
-
-         if (menu_st->flags & MENU_ST_FLAG_ALIVE)
+#ifdef HAVE_SPRUCE_IGM_SW
+         if (spruce_igm_sw_is_enabled()
+               && !(menu_st->flags & MENU_ST_FLAG_ALIVE))
          {
-            if (rarch_is_initialized && !core_type_is_dummy && core_is_running)
-               retroarch_menu_running_finished(false);
+            spruce_igm_sw_toggle();
          }
          else
-            retroarch_menu_running();
+#endif
+         {
+            bool core_is_running    = runloop_st->flags & RUNLOOP_FLAG_CORE_RUNNING;
+
+            if (menu_st->flags & MENU_ST_FLAG_ALIVE)
+            {
+               if (rarch_is_initialized && !core_type_is_dummy && core_is_running)
+                  retroarch_menu_running_finished(false);
+            }
+            else
+               retroarch_menu_running();
+         }
       }
       /* Initial menu toggle on startup */
       else if (core_type_is_dummy && !(menu_st->flags & MENU_ST_FLAG_ALIVE))
@@ -7321,6 +7334,9 @@ int runloop_iterate(void)
          }
 #endif
          video_driver_cached_frame();
+#ifdef HAVE_SPRUCE_IGM_SW
+         spruce_igm_sw_process_pending();
+#endif
          goto end;
       case RUNLOOP_STATE_MENU:
 #if defined(HAVE_MENU) && defined(HAVE_NETWORKING)
--- a/input/input_driver.c
+++ b/input/input_driver.c
@@ -6525,6 +6525,52 @@
    return result;
 }
 
+/**
+ * input_driver_state_raw_joypad:
+ *
+ * Reads a joypad button for @port straight from the input driver, skipping
+ * the pipeline input_driver_state_wrapper() runs for the core: input remaps,
+ * port remapping and turbo.
+ *
+ * Anything that draws its own UI over a running game wants the button the
+ * player physically pressed, which is how RetroArch's own menu reads input.
+ * Going through the core path instead means a core or game remap moves the
+ * UI's buttons around, and a port remap sends them to a different port,
+ * leaving the UI unable to see any input at all.
+ **/
+int16_t input_driver_state_raw_joypad(unsigned port, unsigned id)
+{
+   input_driver_state_t *input_st          = &input_driver_st;
+   settings_t *settings                    = config_get_ptr();
+   const input_device_driver_t *joypad     = input_st->primary_joypad;
+#ifdef HAVE_MFI
+   const input_device_driver_t *sec_joypad = input_st->secondary_joypad;
+#else
+   const input_device_driver_t *sec_joypad = NULL;
+#endif
+   rarch_joypad_info_t joypad_info;
+
+   if (!settings || port >= MAX_USERS)
+      return 0;
+
+   joypad_info.joy_idx                     = settings->uints.input_joypad_index[port];
+   joypad_info.auto_binds                  = input_autoconf_binds[joypad_info.joy_idx];
+   joypad_info.axis_threshold              = settings->floats.input_axis_threshold;
+
+   return (int16_t)input_state_wrap(
+         input_st->current_driver,
+         input_st->current_data,
+         joypad,
+         sec_joypad,
+         &joypad_info,
+         (*input_st->libretro_input_binds),
+         (input_st->flags & INP_FLAG_KB_MAPPING_BLOCKED) ? true : false,
+         port,
+         RETRO_DEVICE_JOYPAD,
+         0,
+         id);
+}
+
 #ifdef HAVE_HID
 void *hid_driver_get_data(void)
 {
--- a/input/input_driver.h
+++ b/input/input_driver.h
@@ -1152,6 +1152,8 @@
 int16_t input_driver_state_wrapper(unsigned port, unsigned device,
       unsigned idx, unsigned id);
 
+int16_t input_driver_state_raw_joypad(unsigned port, unsigned id);
+
 void input_driver_collect_system_input(input_driver_state_t *input_st,
       settings_t *settings, input_bits_t *current_bits);
 
